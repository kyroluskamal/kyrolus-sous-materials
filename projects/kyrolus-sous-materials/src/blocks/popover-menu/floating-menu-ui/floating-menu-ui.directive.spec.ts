import { FloatingMenuUIDirective } from './floating-menu-ui.directive';

describe('FloatingMenuUIDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatingMenuUIDirective();
    expect(directive).toBeTruthy();
  });
});
