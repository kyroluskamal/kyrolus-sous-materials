import { FloatingUiFreeStyleDirective } from './floating-ui-free-style.directive';

describe('FloatingUiFreeStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatingUiFreeStyleDirective();
    expect(directive).toBeTruthy();
  });
});
