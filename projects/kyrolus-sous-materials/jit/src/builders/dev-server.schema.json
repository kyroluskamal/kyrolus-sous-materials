{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "ks JIT Dev Server Builder",
  "description": "Wraps @angular/build:dev-server. Starts the JIT watcher before delegating. Options are forwarded verbatim to @angular/build:dev-server — refer to its schema for the full option list.",
  "type": "object",
  "additionalProperties": true
}
