{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "ks JIT Application Builder",
  "description": "Wraps @angular/build:application. Runs ks JIT before delegating. Options are forwarded verbatim to @angular/build:application — refer to its schema for the full option list.",
  "type": "object",
  "additionalProperties": true
}
